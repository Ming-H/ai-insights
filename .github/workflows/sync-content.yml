name: Sync Content from ContentForge AI

on:
  schedule:
    # 每天多次同步，对应 content-forge-ai 的更新时间
    # 为了避免生成未完成，延后 45 分钟同步
    # 06:00 生成 => 06:45 同步（北京时间），即 UTC 22:45
    - cron: '45 22 * * *'
    # 12:00 生成 => 12:45 同步（北京时间），即 UTC 04:45
    - cron: '45 4 * * *'
    # 18:00 生成 => 18:45 同步（北京时间），即 UTC 10:45
    - cron: '45 10 * * *'
  workflow_dispatch:
  # 允许从 content-forge-ai 仓库触发同步
  repository_dispatch:
    types: [content-updated]

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout ai-insights
        uses: actions/checkout@v4
        with:
          path: ai-insights

      # 检出 content-forge-ai 仓库
      # 注意：如果 content-forge-ai 是私有仓库，需要配置 PAT Token
      - name: Checkout content-forge-ai
        uses: actions/checkout@v4
        with:
          repository: Ming-H/content-forge-ai
          path: content-forge-ai
          # 如果是私有仓库，使用 PAT: ${{ secrets.CONTENT_FORGE_AI_TOKEN }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Verify content sources
        run: |
          echo "========================================"
          echo "验证数据源..."
          echo "========================================"
          echo "ContentForge AI 路径: $GITHUB_WORKSPACE/content-forge-ai"
          echo "AI Insights 路径: $GITHUB_WORKSPACE/ai-insights"
          ls -la $GITHUB_WORKSPACE/content-forge-ai/data/ || echo "⚠️ content-forge-ai/data 目录不存在"
          ls -la $GITHUB_WORKSPACE/ai-insights/content/ || echo "⚠️ ai-insights/content 目录不存在"

      - name: Guard: wait for daily digest to exist (10m max)
        env:
          TZ: Asia/Shanghai
        run: |
          # 计算“今天”在北京时区的日期
          today=$(TZ=$TZ date +%Y%m%d)
          digest_dir="$GITHUB_WORKSPACE/content-forge-ai/data/daily/$today/digest"

          echo "期望的数据目录: $digest_dir"

          for i in $(seq 1 5); do
            if [ -d "$digest_dir" ]; then
              echo "✅ 找到今日 digest 目录，继续同步。"
              echo "has_digest=true" >> $GITHUB_ENV
              exit 0
            fi
            echo "⏳ 未找到今日 digest，等待中... (${i}/5)"
            sleep 120
          done

          echo "⚠️ 等待 10 分钟后仍未找到今日 digest，跳过本次同步。"
          echo "has_digest=false" >> $GITHUB_ENV
          exit 0

      - name: Sync content
        if: env.has_digest != 'false'
        run: |
          cd ai-insights
          echo "========================================"
          echo "开始同步内容..."
          echo "========================================"
          export CONTENT_FORGE_AI_PATH="$GITHUB_WORKSPACE/content-forge-ai"
          export HUGO_CONTENT_PATH="$GITHUB_WORKSPACE/ai-insights/content"
          python scripts/sync_content.py --all
          echo "========================================"
          echo "内容同步完成"
          echo "========================================"

      - name: Check for changes
        id: check-changes
        if: env.has_digest != 'false'
        run: |
          cd ai-insights
          if git diff --quiet && git diff --staged --quiet; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "没有检测到新的内容变更"
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "检测到新的内容变更，准备提交"
            git status
          fi

      - name: Commit and push
        if: env.has_digest != 'false' && steps.check-changes.outputs.has_changes == 'true'
        run: |
          cd ai-insights
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          # 先拉取远程更改，避免冲突
          git pull --rebase origin main || { echo "⚠️ pull --rebase 失败，尝试自动恢复"; git rebase --abort || true; git reset --hard HEAD; git pull --rebase origin main; }
          git add content/
          git commit -m "chore: sync content from content-forge-ai [skip ci]" -m "自动同步时间: $(date '+%Y-%m-%d %H:%M:%S') (UTC)"
          git push
          echo "✅ 内容已成功同步并推送到仓库"

      - name: Summary report
        if: always()
        run: |
          echo "========================================"
          echo "📊 内容同步报告"
          echo "========================================"
          echo "执行时间: $(date '+%Y-%m-%d %H:%M:%S') (UTC)"
          echo "执行状态: ${{ job.status }}"
          echo "有变更: ${{ steps.check-changes.outputs.has_changes }}"
          echo "========================================"
